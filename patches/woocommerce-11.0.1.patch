diff -ruN woocommerce-11.0.1/assets/js/frontend/a8c-address-autocomplete-service.js woocommerce-11.0.1-patched/assets/js/frontend/a8c-address-autocomplete-service.js
--- woocommerce-11.0.1/assets/js/frontend/a8c-address-autocomplete-service.js	2026-07-13 17:35:30.000000000 +0100
+++ woocommerce-11.0.1-patched/assets/js/frontend/a8c-address-autocomplete-service.js	2026-08-11 05:58:08.803271237 +0100
@@ -469,6 +469,8 @@
 }
 
 function createStatsdURL( sectionName, events ) {
+	// Disable address autocomplete tracking pixel to pixel.wp.com.
+	return '';
 	if ( ! Array.isArray( events ) ) {
 		events = [ events ]; // Only a single event was passed to process.
 	}
diff -ruN woocommerce-11.0.1/includes/admin/helper/class-wc-helper.php woocommerce-11.0.1-patched/includes/admin/helper/class-wc-helper.php
--- woocommerce-11.0.1/includes/admin/helper/class-wc-helper.php	2026-02-23 12:58:34.000000000 +0000
+++ woocommerce-11.0.1-patched/includes/admin/helper/class-wc-helper.php	2026-08-11 05:58:08.803271237 +0100
@@ -1733,6 +1733,8 @@
 	 * phpcs:ignore Squiz.Commenting.FunctionCommentThrowTag.Missing -- As we wrap the throw in a try/catch.
 	 */
 	public static function get_product_usage_notice_rules() {
+        return []; // Stop calling home when we don't need to.
+        
 		$cache_key = '_woocommerce_helper_product_usage_notice_rules';
 		$data      = get_transient( $cache_key );
 		if ( false !== $data ) {
diff -ruN woocommerce-11.0.1/includes/admin/marketplace-suggestions/class-wc-marketplace-suggestions.php woocommerce-11.0.1-patched/includes/admin/marketplace-suggestions/class-wc-marketplace-suggestions.php
--- woocommerce-11.0.1/includes/admin/marketplace-suggestions/class-wc-marketplace-suggestions.php	2025-07-29 08:34:58.000000000 +0100
+++ woocommerce-11.0.1-patched/includes/admin/marketplace-suggestions/class-wc-marketplace-suggestions.php	2026-08-11 05:58:08.803271237 +0100
@@ -163,6 +163,9 @@
 	 * @return bool
 	 */
 	public static function allow_suggestions() {
+        // 2025-09-22 PF. Just don't.
+        return false;
+        
 		// We currently only support English suggestions.
 		$locale             = get_locale();
 		$suggestion_locales = array(
diff -ruN woocommerce-11.0.1/includes/class-wc-payment-gateways.php woocommerce-11.0.1-patched/includes/class-wc-payment-gateways.php
--- woocommerce-11.0.1/includes/class-wc-payment-gateways.php	2026-07-13 17:35:30.000000000 +0100
+++ woocommerce-11.0.1-patched/includes/class-wc-payment-gateways.php	2026-08-11 05:58:08.803271237 +0100
@@ -430,6 +430,14 @@
 	 * @return void
 	 */
 	private function record_gateway_event( string $name, $gateway ) {
+		// wpatcher: disable gateway enable/disable Tracks telemetry AND fix a fatal.
+		// This method derefs WC()->countries->get_base_country(), but WC()->countries is null
+		// until WooCommerce::init() (WP `init` priority 0). Any plugin that writes a payment-gateway
+		// option before that point reaches here with a null WC()->countries -> "Call to a member
+		// function get_base_country() on null". (Observed trigger: PPCP fires its settings migration
+		// on `init` priority -1.) The method only ends in wc_admin_record_tracks_event(), so a plain
+		// early return both kills the crash and suppresses the phone-home.
+		return;
 		if ( ! function_exists( 'wc_admin_record_tracks_event' ) ) {
 			return;
 		}
diff -ruN woocommerce-11.0.1/includes/class-woocommerce.php woocommerce-11.0.1-patched/includes/class-woocommerce.php
--- woocommerce-11.0.1/includes/class-woocommerce.php	2026-08-10 17:52:16.000000000 +0100
+++ woocommerce-11.0.1-patched/includes/class-woocommerce.php	2026-08-11 05:58:08.803271237 +0100
@@ -322,7 +322,7 @@
 
 		add_action( 'plugins_loaded', array( $this, 'on_plugins_loaded' ), -1 );
 		add_action( 'plugins_loaded', array( $this, 'init_customizer' ) );
-		add_action( 'plugins_loaded', array( $this, 'init_jetpack_connection_config' ), 1 );
+		// add_action( 'plugins_loaded', array( $this, 'init_jetpack_connection_config' ), 1 );
 		add_action( 'admin_notices', array( $this, 'build_dependencies_notice' ) );
 		add_action( 'after_setup_theme', array( $this, 'setup_environment' ) );
 		add_action( 'after_setup_theme', array( $this, 'include_template_functions' ), 11 );
@@ -336,7 +336,18 @@
 		add_action( 'init', array( $this, 'add_image_sizes' ) );
 		add_action( 'init', array( $this, 'load_rest_api' ) );
 		if ( $this->is_request( 'admin' ) || ( $this->is_rest_api_request() && ! $this->is_store_api_request() ) || ( defined( 'WP_CLI' ) && WP_CLI ) ) {
-			add_action( 'init', array( 'WC_Site_Tracking', 'init' ) );
+            // Even with site usage tracking disabled, Woo still puts loads of tracking stuff into the browser.
+            // Let's get rid of that here.
+			// add_action( 'init', array( 'WC_Site_Tracking', 'init' ) );
+            // We need a stub so that some secondary scripts don't generate errors.
+            add_action( 'admin_footer', function(){
+                echo '<script type="text/javascript">';
+                echo 'window.wcTracks = window.wcTracks || {};';
+                echo 'window.wcTracks.isEnabled = false;';
+                echo 'window.wcTracks.validateEvent = () => {};';
+                echo 'window.wcTracks.recordEvent = () => {};';
+                echo '</script>';
+            });
 		}
 		add_action( 'switch_blog', array( $this, 'wpdb_table_fix' ), 0 );
 		add_action( 'activated_plugin', array( $this, 'activated_plugin' ) );
@@ -776,8 +787,8 @@
 		include_once WC_ABSPATH . 'includes/class-wc-logger.php';
 		include_once WC_ABSPATH . 'includes/queue/class-wc-action-queue.php';
 		include_once WC_ABSPATH . 'includes/queue/class-wc-queue.php';
-		include_once WC_ABSPATH . 'includes/admin/marketplace-suggestions/class-wc-marketplace-updater.php';
-		include_once WC_ABSPATH . 'includes/admin/class-wc-admin-marketplace-promotions.php';
+		// include_once WC_ABSPATH . 'includes/admin/marketplace-suggestions/class-wc-marketplace-updater.php';
+		// include_once WC_ABSPATH . 'includes/admin/class-wc-admin-marketplace-promotions.php';
 		include_once WC_ABSPATH . 'includes/blocks/class-wc-blocks-utils.php';
 
 		/**
@@ -958,6 +969,38 @@
 		 */
 		do_action( 'before_woocommerce_init' ); // phpcs:ignore WooCommerce.Commenting.CommentHooks.MissingSinceComment
 
+        // Force some options to no and autoload them.
+        // 'woocommerce_feature_order_attribution_enabled',
+        $options_to_no = [
+            'woocommerce_allow_tracking',
+            'marketplace_suggestions',
+            'woocommerce_show_marketplace_suggestions',
+            'woocommerce_feature_remote_logging_enabled',
+            'woocommerce_feature_reactify-classic-payments-settings_enabled',
+            'woocommerce_feature_blueprint_enabled',
+            'woocommerce_feature_point_of_sale_enabled'
+        ];
+        foreach( $options_to_no as $option_name ) {
+            if( filter_var( get_option( $option_name, false ),  FILTER_VALIDATE_BOOLEAN ) !== false ) {
+                update_option( $option_name, 'no', true );
+            }
+        }
+
+        // Force the remote variant assignment to 0 and autoload it.
+        // This option is a sticky random 1-120 bucket that WooCommerce assigns at install
+        // (WooCommerce::add_woocommerce_remote_variant, hooked to woocommerce_installed and
+        // woocommerce_updated). Remote Spec Engines test it with a numeric 'range' rule to
+        // roll features and plugin recommendations out to a percentage of stores without
+        // asking -- e.g. the variation gallery canary (1-6), the MailPoet/Klaviyo split
+        // (1-84 / 85-120), and the TikTok/Pinterest split (1-60 / 61-120).
+        // 0 sits below every range core defines, so this store matches no cohort.
+        // Note we also write when the option is absent: add_woocommerce_remote_variant only
+        // rolls when get_option() returns false, so storing '0' pre-empts the roll instead of
+        // letting each update re-roll and having to correct it on the following request.
+        if( '0' !== (string) get_option( 'woocommerce_remote_variant_assignment', false ) ) {
+            update_option( 'woocommerce_remote_variant_assignment', 0, true );
+        }
+
 		// Set up localisation.
 		$this->load_plugin_textdomain();
 
@@ -1610,6 +1653,9 @@
 	 * @internal For exclusive usage of WooCommerce core, backwards compatibility not guaranteed.
 	 */
 	public function add_woocommerce_tracker_send_event_wrapper() {
+        // Never add tracking. Avoid leaking site data and consuming server resource.
+        return;
+        
 		if ( true !== wc_string_to_bool( get_option( 'woocommerce_allow_tracking', 'no' ) ) ) {
 			return;
 		}
diff -ruN woocommerce-11.0.1/includes/react-admin/class-experimental-abtest.php woocommerce-11.0.1-patched/includes/react-admin/class-experimental-abtest.php
--- woocommerce-11.0.1/includes/react-admin/class-experimental-abtest.php	2025-03-03 17:28:12.000000000 +0000
+++ woocommerce-11.0.1-patched/includes/react-admin/class-experimental-abtest.php	2026-08-11 05:58:08.803271237 +0100
@@ -153,6 +153,9 @@
 	 * @return array|\WP_Error A/B test variation error on failure.
 	 */
 	public function request_assignment( $args ) {
+		// No A/B test assignments from WordPress.com. Return control variant.
+		return array( 'body' => wp_json_encode( array( 'variations' => array() ) ) );
+
 		// Request as authenticated wp user.
 		if ( $this->as_auth_wpcom_user && class_exists( Jetpack_Connection_Manager::class ) ) {
 			$jetpack_connection_manager = new Jetpack_Connection_Manager();
diff -ruN woocommerce-11.0.1/includes/tracks/class-wc-site-tracking.php woocommerce-11.0.1-patched/includes/tracks/class-wc-site-tracking.php
--- woocommerce-11.0.1/includes/tracks/class-wc-site-tracking.php	2026-02-23 12:58:34.000000000 +0000
+++ woocommerce-11.0.1-patched/includes/tracks/class-wc-site-tracking.php	2026-08-11 05:58:08.803271237 +0100
@@ -17,6 +17,9 @@
 	 * @return bool
 	 */
 	public static function is_tracking_enabled() {
+        // Never use remote logging
+        return false;
+        
 		/**
 		 * Don't track users if a filter has been applied to turn it off.
 		 * `woocommerce_apply_tracking` will be deprecated. Please use
diff -ruN woocommerce-11.0.1/includes/tracks/class-wc-tracks-client.php woocommerce-11.0.1-patched/includes/tracks/class-wc-tracks-client.php
--- woocommerce-11.0.1/includes/tracks/class-wc-tracks-client.php	2026-01-19 09:46:18.000000000 +0000
+++ woocommerce-11.0.1-patched/includes/tracks/class-wc-tracks-client.php	2026-08-11 05:58:08.803271237 +0100
@@ -50,6 +50,9 @@
 	 * @return void
 	 */
 	public static function init() {
+        // Abaort - never set the identity cookie.
+        return;
+        
 		// Use wp hook for setting the identity cookie to avoid headers already sent warnings.
 		add_action( 'admin_init', array( __CLASS__, 'maybe_set_identity_cookie' ) );
 	}
diff -ruN woocommerce-11.0.1/includes/wccom-site/class-wc-wccom-site.php woocommerce-11.0.1-patched/includes/wccom-site/class-wc-wccom-site.php
--- woocommerce-11.0.1/includes/wccom-site/class-wc-wccom-site.php	2024-12-18 17:19:16.000000000 +0000
+++ woocommerce-11.0.1-patched/includes/wccom-site/class-wc-wccom-site.php	2026-08-11 05:58:08.803271237 +0100
@@ -26,6 +26,9 @@
 	 * @since 3.7.0
 	 */
 	public static function load() {
+        // Disable remote product installation from WooCommerce.com
+        return;
+        
 		self::includes();
 
 		add_action( 'woocommerce_wccom_install_products', array( 'WC_WCCOM_Site_Installer', 'install' ) );
diff -ruN woocommerce-11.0.1/src/Admin/Features/MarketingRecommendations/MarketingRecommendationsDataSourcePoller.php woocommerce-11.0.1-patched/src/Admin/Features/MarketingRecommendations/MarketingRecommendationsDataSourcePoller.php
--- woocommerce-11.0.1/src/Admin/Features/MarketingRecommendations/MarketingRecommendationsDataSourcePoller.php	2024-11-13 20:17:00.000000000 +0000
+++ woocommerce-11.0.1-patched/src/Admin/Features/MarketingRecommendations/MarketingRecommendationsDataSourcePoller.php	2026-08-11 05:58:08.807271291 +0100
@@ -51,6 +51,8 @@
 	 * @return array
 	 */
 	public static function get_data_sources() {
+		return []; // No phoning home for marketing recommendations.
+
 		return array(
 			WC_Helper::get_woocommerce_com_base_url() . 'wp-json/wccom/marketing-tab/1.3/recommendations.json',
 		);
diff -ruN woocommerce-11.0.1/src/Admin/Features/MarketingRecommendations/MiscRecommendationsDataSourcePoller.php woocommerce-11.0.1-patched/src/Admin/Features/MarketingRecommendations/MiscRecommendationsDataSourcePoller.php
--- woocommerce-11.0.1/src/Admin/Features/MarketingRecommendations/MiscRecommendationsDataSourcePoller.php	2024-11-13 20:17:00.000000000 +0000
+++ woocommerce-11.0.1-patched/src/Admin/Features/MarketingRecommendations/MiscRecommendationsDataSourcePoller.php	2026-08-11 05:58:08.807271291 +0100
@@ -59,6 +59,8 @@
 	 * @return array
 	 */
 	public static function get_data_sources() {
+		return []; // No phoning home for misc recommendations.
+
 		return array(
 			WC_Helper::get_woocommerce_com_base_url() . 'wp-json/wccom/marketing-tab/misc/recommendations.json',
 		);
diff -ruN woocommerce-11.0.1/src/Admin/Features/PaymentGatewaySuggestions/PaymentGatewaySuggestionsDataSourcePoller.php woocommerce-11.0.1-patched/src/Admin/Features/PaymentGatewaySuggestions/PaymentGatewaySuggestionsDataSourcePoller.php
--- woocommerce-11.0.1/src/Admin/Features/PaymentGatewaySuggestions/PaymentGatewaySuggestionsDataSourcePoller.php	2024-11-13 20:17:00.000000000 +0000
+++ woocommerce-11.0.1-patched/src/Admin/Features/PaymentGatewaySuggestions/PaymentGatewaySuggestionsDataSourcePoller.php	2026-08-11 05:58:08.807271291 +0100
@@ -45,6 +45,8 @@
 	 * @return array
 	 */
 	public static function get_data_sources() {
+        return []; // 2026-03-28 PF. Short-circuit
+        
 		$data_sources = array(
 			WC_Helper::get_woocommerce_com_base_url() . 'wp-json/wccom/payment-gateway-suggestions/2.0/suggestions.json',
 		);
diff -ruN woocommerce-11.0.1/src/Admin/Features/ShippingPartnerSuggestions/ShippingPartnerSuggestionsDataSourcePoller.php woocommerce-11.0.1-patched/src/Admin/Features/ShippingPartnerSuggestions/ShippingPartnerSuggestionsDataSourcePoller.php
--- woocommerce-11.0.1/src/Admin/Features/ShippingPartnerSuggestions/ShippingPartnerSuggestionsDataSourcePoller.php	2024-11-13 20:17:00.000000000 +0000
+++ woocommerce-11.0.1-patched/src/Admin/Features/ShippingPartnerSuggestions/ShippingPartnerSuggestionsDataSourcePoller.php	2026-08-11 05:58:08.807271291 +0100
@@ -45,6 +45,8 @@
 	 * @return array
 	 */
 	public static function get_data_sources() {
+		return []; // No phoning home for shipping partner suggestions.
+
 		return array(
 			WC_Helper::get_woocommerce_com_base_url() . 'wp-json/wccom/shipping-partner-suggestions/2.0/suggestions.json',
 		);
diff -ruN woocommerce-11.0.1/src/Admin/RemoteInboxNotifications/RemoteInboxNotificationsDataSourcePoller.php woocommerce-11.0.1-patched/src/Admin/RemoteInboxNotifications/RemoteInboxNotificationsDataSourcePoller.php
--- woocommerce-11.0.1/src/Admin/RemoteInboxNotifications/RemoteInboxNotificationsDataSourcePoller.php	2026-05-05 10:26:50.000000000 +0100
+++ woocommerce-11.0.1-patched/src/Admin/RemoteInboxNotifications/RemoteInboxNotificationsDataSourcePoller.php	2026-08-11 05:58:08.807271291 +0100
@@ -232,6 +232,8 @@
 	 * @return array
 	 */
 	public static function get_data_sources() {
+		return []; // No phoning home for remote inbox notifications.
+
 		return array(
 			WC_Helper::get_woocommerce_com_base_url() . 'wp-json/wccom/inbox-notifications/2.0/notifications.json',
 		);
diff -ruN woocommerce-11.0.1/src/Blocks/DependencyDetection.php woocommerce-11.0.1-patched/src/Blocks/DependencyDetection.php
--- woocommerce-11.0.1/src/Blocks/DependencyDetection.php	2026-07-13 17:35:30.000000000 +0100
+++ woocommerce-11.0.1-patched/src/Blocks/DependencyDetection.php	2026-08-11 05:58:08.807271291 +0100
@@ -36,7 +36,7 @@
 	 * Maps window.wc.* property names to their required script handles.
 	 *
 	 * This is the source of truth for both PHP and JS dependency detection.
-	 * Based on wcDepMap and wcHandleMap in client/blocks/bin/webpack-helpers.js.
+	 * Based on wcDepMap and wcHandleMap
 	 *
 	 * @var array<string, string>
 	 */
diff -ruN woocommerce-11.0.1/src/Internal/Admin/RemoteFreeExtensions/RemoteFreeExtensionsDataSourcePoller.php woocommerce-11.0.1-patched/src/Internal/Admin/RemoteFreeExtensions/RemoteFreeExtensionsDataSourcePoller.php
--- woocommerce-11.0.1/src/Internal/Admin/RemoteFreeExtensions/RemoteFreeExtensionsDataSourcePoller.php	2024-11-13 20:17:00.000000000 +0000
+++ woocommerce-11.0.1-patched/src/Internal/Admin/RemoteFreeExtensions/RemoteFreeExtensionsDataSourcePoller.php	2026-08-11 05:58:08.807271291 +0100
@@ -47,6 +47,8 @@
 	 * @return array
 	 */
 	public static function get_data_sources() {
+        return []; // PF: No phoning home for extension suggestions.
+        
 		return array(
 			WC_Helper::get_woocommerce_com_base_url() . 'wp-json/wccom/obw-free-extensions/4.0/extensions.json',
 		);
diff -ruN woocommerce-11.0.1/src/Internal/Admin/Settings/Payments.php woocommerce-11.0.1-patched/src/Internal/Admin/Settings/Payments.php
--- woocommerce-11.0.1/src/Internal/Admin/Settings/Payments.php	2026-03-12 16:10:34.000000000 +0000
+++ woocommerce-11.0.1-patched/src/Internal/Admin/Settings/Payments.php	2026-08-11 05:58:08.807271291 +0100
@@ -88,7 +88,7 @@
 		// Only include suggestions if the requesting user can install plugins.
 		$suggestions = array();
 		if ( current_user_can( 'install_plugins' ) ) {
-			$suggestions = $this->providers->get_extension_suggestions( $location, self::SUGGESTIONS_CONTEXT );
+			// $suggestions = $this->providers->get_extension_suggestions( $location, self::SUGGESTIONS_CONTEXT );
 		}
 		// If we have preferred suggestions, add them to the providers list.
 		if ( ! empty( $suggestions['preferred'] ) ) {
diff -ruN woocommerce-11.0.1/src/Internal/Admin/Settings/PaymentsProviders.php woocommerce-11.0.1-patched/src/Internal/Admin/Settings/PaymentsProviders.php
--- woocommerce-11.0.1/src/Internal/Admin/Settings/PaymentsProviders.php	2026-05-05 10:26:50.000000000 +0100
+++ woocommerce-11.0.1-patched/src/Internal/Admin/Settings/PaymentsProviders.php	2026-08-11 05:58:08.807271291 +0100
@@ -651,6 +651,9 @@
 	 * @throws Exception If there are malformed or invalid suggestions.
 	 */
 	public function get_extension_suggestions( string $location, string $context = '' ): array {
+        // 2025-09-22 PF. Just don't.
+        return array( 'preferred' => [], 'other' => [] );
+
 		// Normalize the location to uppercase.
 		$location = strtoupper( $location );
 
diff -ruN woocommerce-11.0.1/src/Internal/Admin/WCAdminAssets.php woocommerce-11.0.1-patched/src/Internal/Admin/WCAdminAssets.php
--- woocommerce-11.0.1/src/Internal/Admin/WCAdminAssets.php	2026-07-13 17:35:30.000000000 +0100
+++ woocommerce-11.0.1-patched/src/Internal/Admin/WCAdminAssets.php	2026-08-11 05:58:08.807271291 +0100
@@ -247,6 +247,65 @@
 		wp_enqueue_script( WC_ADMIN_APP );
 		wp_enqueue_style( WC_ADMIN_APP );
 
+		// DEBLOAT: short-circuit the wc-admin Jetpack connection check on the client.
+		// The isJetpackConnected resolver unconditionally calls apiFetch( '/jetpack/v4/connection' )
+		// on every wc-admin page load. With the Jetpack connection disabled the route is never
+		// registered, so that request 404s (log noise) and needlessly boots WP/PHP to answer it.
+		// Registering an api-fetch middleware that resolves the path locally as "not connected"
+		// avoids the request entirely — no network round-trip, no PHP, no 404. Only the exact
+		// status endpoint is intercepted; any other path falls through to next() unchanged.
+		if ( wp_script_is( 'wp-api-fetch', 'registered' ) ) {
+			$wc_debloat_jetpack_js = <<<'JS'
+;( function ( wp ) {
+	if ( ! wp || ! wp.apiFetch || ! wp.apiFetch.use ) { return; }
+	wp.apiFetch.use( function ( options, next ) {
+		var path = ( options && options.path ) || '';
+		var clean = ( typeof path === 'string' ? path : '' ).split( '?' )[0].replace( /\/+$/, '' );
+		if ( /(^|\/)jetpack\/v4\/connection$/.test( clean ) ) {
+			return Promise.resolve( { isActive: false, isStaging: false, isRegistered: false, isUserConnected: false, hasConnectedOwner: false, offlineMode: { isActive: false }, isPublic: true } );
+		}
+		return next( options );
+	} );
+} )( window.wp );
+JS;
+			wp_add_inline_script( 'wp-api-fetch', $wc_debloat_jetpack_js, 'after' );
+		}
+
+		// DEBLOAT: suppress the two Marketplace upsell surfaces on the Extensions screen.
+		//   - .woocommerce-marketplace__banner: a four-slide rotating carousel above the page.
+		//   - .woocommerce-marketplace__footer: the same four messages again at the bottom,
+		//     under "Hundreds of vetted products and services. Unlimited potential."
+		// Both are "30-day money-back guarantee" / "Get help when you need it" / "Products you
+		// can trust" / "Support the ecosystem", linking out to woocommerce.com and firing
+		// marketplace_*_link_click Tracks events. Both are hardcoded in the compiled app bundle:
+		// no remote fetch, no PHP hook, no filter, and no code path by which a store notice or
+		// admin alert can ever appear in either. Notices and store alerts render in a separate
+		// subtree, so nothing operational is lost by removing them.
+		// There is no server-side hook to unhook, so this is done on the client. Each class has
+		// exactly one use in the bundle and one rule block in the stylesheet, so the CSS cannot
+		// catch anything else. The banner additionally gets its own dismissal flag seeded, so it
+		// returns null and emits no DOM rather than merely being hidden -- the CSS covers the
+		// flash before that effect runs. That flag is the same one the built-in dismiss button
+		// sets, so it is equivalent to the merchant having clicked the X, and it persists in the
+		// browser: if this patch is later removed, clear localStorage key
+		// wc_featuredBannerDismissed to bring the banner back.
+		// The leading semicolon is load-bearing. WordPress concatenates all 'before' inline
+		// scripts for a handle with newlines, and Features.php emits
+		// "window.wcAdminFeatures = {...}" with no trailing semicolon. Without the ';' here,
+		// ASI does not break the statement -- "{...}" followed by "(" parses as a call with the
+		// object as the callee, so wcAdminFeatures is never assigned and the whole wc-admin app
+		// dies with "({...}) is not a function". Do not remove it.
+		$wc_debloat_banner_js = <<<'JS'
+;( function () {
+	try { window.localStorage.setItem( 'wc_featuredBannerDismissed', 'true' ); } catch ( e ) {}
+} )();
+JS;
+		wp_add_inline_script( WC_ADMIN_APP, $wc_debloat_banner_js, 'before' );
+		wp_add_inline_style(
+			WC_ADMIN_APP,
+			'.woocommerce-marketplace__banner,.woocommerce-marketplace__footer{display:none!important;}'
+		);
+
 		wp_enqueue_style( 'wc-material-icons' );
 		wp_enqueue_style( 'wc-onboarding' );
 
diff -ruN woocommerce-11.0.1/src/Internal/VariationGallery/Package.php woocommerce-11.0.1-patched/src/Internal/VariationGallery/Package.php
--- woocommerce-11.0.1/src/Internal/VariationGallery/Package.php	2026-07-13 17:35:30.000000000 +0100
+++ woocommerce-11.0.1-patched/src/Internal/VariationGallery/Package.php	2026-08-11 05:58:08.807271291 +0100
@@ -56,6 +56,15 @@
 	 * @return bool
 	 */
 	public static function is_in_canary_cohort(): bool {
+		// wpatcher: never auto-enrol this store in the variation gallery canary rollout.
+		// WC 11.0.0 changed the feature's 'enabled_by_default' from a plain false to this
+		// method, so ~5% of stores (variant 1-6 of a random 1-120 assigned at install/update)
+		// silently switch their product gallery UI on upgrade, with no merchant action.
+		// Returning false restores the 10.9.x default. Note this is deliberately patched
+		// here rather than in is_enabled() -- an explicit 'yes' on ENABLE_OPTION_NAME still
+		// wins, so a merchant who opts in via Settings -> Advanced -> Features keeps the feature.
+		return false;
+
 		$variant_assignment = (int) get_option( self::REMOTE_VARIANT_OPTION_NAME, 0 );
 		return $variant_assignment > 0 && $variant_assignment <= self::CANARY_MAX_VARIANT;
 	}
diff -ruN woocommerce-11.0.1/vendor/automattic/jetpack-connection/src/class-plugin.php woocommerce-11.0.1-patched/vendor/automattic/jetpack-connection/src/class-plugin.php
--- woocommerce-11.0.1/vendor/automattic/jetpack-connection/src/class-plugin.php	2025-09-01 19:44:48.000000000 +0100
+++ woocommerce-11.0.1-patched/vendor/automattic/jetpack-connection/src/class-plugin.php	2026-08-11 05:58:08.807271291 +0100
@@ -47,7 +47,7 @@
 		$this->slug = $slug;
 
 		// Initialize Users_Connection_Admin
-		$this->users_connection_admin = new Users_Connection_Admin();
+		// $this->users_connection_admin = new Users_Connection_Admin();
 	}
 
 	/**
diff -ruN woocommerce-11.0.1/woocommerce.php woocommerce-11.0.1-patched/woocommerce.php
--- woocommerce-11.0.1/woocommerce.php	2026-08-10 17:52:16.000000000 +0100
+++ woocommerce-11.0.1-patched/woocommerce.php	2026-08-11 06:02:20.914678231 +0100
@@ -73,3 +73,18 @@
 if ( class_exists( \Automattic\Jetpack\Connection\Rest_Authentication::class ) ) {
 	\Automattic\Jetpack\Connection\Rest_Authentication::init();
 }
+
+// START : wpatcher
+add_filter(
+	'plugin_row_meta',
+	function ( $links, $file ) {
+		if ( $file == 'woocommerce/woocommerce.php' ) {
+			$links['wpatcher'] = '<span style="background-color: purple; color: white; padding:0.2em 0.5em; font-weight: 700; border-radius: 0.3em">PATCHED : 2026-08-11</span>';
+		}
+
+		return $links;
+	},
+	10,
+	2
+);
+// END : wpatcher
